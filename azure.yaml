# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: bigquery-cosmos-sync-job
metadata:
  template: bigquery-cosmos-sync-job@0.1.0
services:
  sync:
    project: py/apps/bq-cosmos-sync
    language: py
    host: containerapp
    docker:
      path: Dockerfile
      context: ../../..
      remoteBuild: true
infra:
  provider: terraform
  path: infra
